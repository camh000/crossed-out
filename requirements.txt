# Build dependency only. pygame-ce belongs in requirements-dev.txt so that
# Vercel's auto-install does NOT put pygame's bundled examples (including
# an mp3 that pygbag refuses to ship to web) into .vercel_python_packages
# where the optimizer would find them. pygbag bundles its own pygame
# runtime for the browser, so we don't need pygame on the build host.
pygbag>=0.9.0
