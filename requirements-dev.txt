# Local development deps. Install with:
#   pip install -r requirements-dev.txt
# pygame-ce is the runtime; pygbag builds the web bundle; pytest runs the tests.
pygame-ce>=2.4.0
pygbag>=0.9.0
pytest>=7.0
