[pygbag]
# Files / directories to exclude from the WebAssembly bundle. The patterns are
# glob-style, matched against paths under the project root.
exclude = tests/* tests/**/* .pytest_cache/* requirements-dev.txt vercel.json .vercelignore AGENTS.md

# User-facing window title used by pygbag's HTML template.
title = Crossed Out
